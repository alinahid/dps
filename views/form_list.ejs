<%- include('partials/admin_header')%>
 <div class="formList">
    <% Users.forEach(function(user){ %>
        <ul>
            <li><a href="/form/<%= user.id%>" > <%= user.name %></a></li>
        <% }); %>
        </ul>
 </div>
    
</body>
</html>